apiVersion: v1
kind: Service
metadata:
  name: inference-gateway
  namespace: inference-system
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: inference-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
