# Security policy

Report vulnerabilities privately to the repository owner.

Never commit credentials, API keys, tokens, or populated environment files. AWS Bedrock is disabled by default and requires both an explicit model ID and a positive session budget. Future paid providers must preserve this fail-closed behavior.
